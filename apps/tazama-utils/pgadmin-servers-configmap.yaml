apiVersion: v1
kind: ConfigMap
metadata:
  name: pgadmin-servers
  namespace: tazama
data:
  servers.json: |
    {
      "Servers": {
        "1": {
          "Name": "Tazama",
          "Group": "Servers",
          "Port": 5432,
          "Username": "postgres",
          "Host": "postgres",
          "MaintenanceDB": "postgres",
          "ConnectionParameters": {
            "sslmode": "prefer",
            "connect_timeout": 10
          }
        }
      }
    }
